diff --git a/ioc/sitehooks.cpp b/ioc/sitehooks.cpp
index 4bd833f..deab920 100644
--- a/ioc/sitehooks.cpp
+++ b/ioc/sitehooks.cpp
@@ -15,6 +15,9 @@ namespace pvxs {
 namespace ioc {
 namespace site {
 
+// Forward declarations: each extension's registrar, defined in its own .cpp.
+void registerAlarmmsg();
+
 namespace {
 // Function-local static variables avoid init-order issues with site-specific registrars.
 std::vector<std::function<void()>>& hooksAtBeginning() {
@@ -44,6 +47,7 @@ void siteHookDispatch(initHookState state) noexcept
 
 void registerHooks()
 {
+    registerAlarmmsg();
     initHookRegister(siteHookDispatch);
 }
 
