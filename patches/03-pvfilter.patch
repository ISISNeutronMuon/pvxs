diff --git a/ioc/Makefile b/ioc/Makefile
index c4d2944..2b99292 100644
--- a/ioc/Makefile
+++ b/ioc/Makefile
@@ -31,3 +31,4 @@ SHRLIB_VERSION = $(PVXS_MAJOR_VERSION).$(PVXS_MINOR_VERSION)
+pvxsIoc_SRCS += pvfilter.cpp
 
 ifdef BASE_3_15
 
diff --git a/ioc/groupsource.cpp b/ioc/groupsource.cpp
index adfc35c..84b1012 100644
--- a/ioc/groupsource.cpp
+++ b/ioc/groupsource.cpp
@@ -28,7 +28,11 @@
+#include "sitehooks.h"
 #include "securitylogger.h"
 #include "securityclient.h"
 #include "localfieldlog.h"
 
+// include last to avoid clash of #define printf with other headers
+#include <epicsStdio.h>
+
 namespace pvxs {
 namespace ioc {
 
@@ -67,3 +71,5 @@ void onPutGroup(Group& group, bool atomic, TriState forceProcessing,
+static
+bool fieldAccessAllowed(const Field& field, const std::string& peerAddr);
 
 /**
  * Constructor for GroupSource registrar.
@@ -85,4 +91,23 @@ GroupSource::GroupSource()
+            auto& group = groupMapEntry.second;
             names->insert(groupName);
+
+            // Q:pva:access="disable"/"loopback_only" is enforced per-field within
+            // a group (see onGet/onPutGroup/onSubscribe below), but warn here
+            // anyway so the .db author sees it at load time, before any
+            // client actually touches the field.
+            for (auto& field: group.fields) {
+                if (!field.value)
+                    continue;
+                const char* recName = dbChannelRecord(field.value)->name;
+                if (site::isPvDisabled(recName))
+                    fprintf(stderr, "%s.%s Warning: record \"%s\" has Q:pva:access=\"disable\"; "
+                                    "this group field will be omitted from GET and reject PUT\n",
+                            groupName.c_str(), field.fullName.c_str(), recName);
+                if (site::isPvLoopbackOnly(recName))
+                    fprintf(stderr, "%s.%s Warning: record \"%s\" has Q:pva:access=\"loopback_only\"; "
+                                    "this group field will be restricted to loopback clients\n",
+                            groupName.c_str(), field.fullName.c_str(), recName);
+            }
         }
     }
 
@@ -398,3 +423,4 @@ void subscriptionPropertiesCallback(void* userArg, dbChannel* pChannel,
+    auto peerAddr(subscriptionOperation->peerName());
     groupSubscriptionCtx->subscriptionControl = subscriptionOperation
             ->connect(groupSubscriptionCtx->group.valueTemplate);
 
@@ -422,3 +448,11 @@ void GroupSource::onSubscribe(const std::shared_ptr<GroupSourceSubscriptionCtx>&
+
+        } else if(!fieldAccessAllowed(field, peerAddr)) {
+            // Q:pva:access: don't subscribe this field for this requester.
+            // Unlike the !field.value case above, this field does have a dbChannel, so the
+            // FieldSubscriptionCtx ctor didn't already mark it primed -- do so here, or
+            // subscriptionPost()'s priming wait would block forever on an event that never comes.
+            fieldSubscriptionContext.hadValueEvent = fieldSubscriptionContext.hadPropertyEvent = true;
+            continue;
         }
 
         auto leafNode = field.findIn(groupSubscriptionCtx->currentValue);
@@ -453,3 +487,16 @@ void GroupSource::onSubscribe(const std::shared_ptr<GroupSourceSubscriptionCtx>&
+/**
+ * Returns true if this group field's own record (if any) allows access from peerAddr,
+ * per Q:pva:access (see pvfilter.cpp). Fields with no associated
+ * dbChannel (Const, Structure, ...) are never restricted by these per-record filters.
+ *
+ * @param field the group field to check
+ * @param peerAddr the requesting client's address, "X.X.X.X:port" (IPv4) or "[addr]:port" (IPv6)
+ */
+static
+bool fieldAccessAllowed(const Field& field, const std::string& peerAddr) {
+    return !field.value || site::isChannelAllowed(dbChannelRecord(field.value)->name, peerAddr.c_str());
+}
+
 static
 bool getGroupField(const Field& field, Value valueTarget, const std::string& groupName,
         const std::unique_ptr<server::ExecOp>& getOperation) {
@@ -497,3 +544,7 @@ void onGet(Group& group, const std::unique_ptr<server::ExecOp>& getOperation) {
+            // Q:pva:access: leave this field at its empty/default
+            // value rather than reading it, per-field, for this requester
+            if(!fieldAccessAllowed(field, getOperation->peerName()))
+                continue;
             // find the leaf node in which to set the value
             auto leafNode = field.findIn(returnValue);
 
@@ -515,3 +566,7 @@ void onGet(Group& group, const std::unique_ptr<server::ExecOp>& getOperation) {
+            // Q:pva:access: leave this field at its empty/default
+            // value rather than reading it, per-field, for this requester
+            if(!fieldAccessAllowed(field, getOperation->peerName()))
+                continue;
 
             // find the leaf node in which to set the value
             auto leafNode = field.findIn(returnValue);
@@ -549,15 +604,19 @@ bool putGroupField(const Value& value,
                    TriState forceProcessing,
                    const SecurityClient& securityClient,
                    const GroupSecurityCache& groupSecurityCache,
-                   server::RemoteLogger& notify) {
+                   server::ExecOp& notify) {
     // find the leaf node that the field refers to in the given value
     auto leafNode = field.findIn(value);
     bool putable = field.info.putOrder!=std::numeric_limits<int64_t>::min();
     bool marked = leafNode.isMarked(true, true) && field.value;
-    bool changing = marked && putable;
+    // Q:pva:access: deny writes to this field for this requester
+    bool allowed = fieldAccessAllowed(field, notify.peerName());
+    bool changing = marked && putable && allowed;
 
     if(marked && !putable) {
         notify.logRemote(Level::Warn, SB()<<field.fieldName<<": no putorder, ignore write");
+    } else if(marked && putable && !allowed) {
+        notify.logRemote(Level::Warn, SB()<<field.fieldName<<": Q:pva:access restricted, ignore write");
     }
 
     // If the field references a valid part of the given value then we can send it to the database
@@ -565,7 +624,7 @@ bool putGroupField(const Value& value,
         IOCSource::doFieldPreProcessing(securityClient); // pre-process field
         IOCSource::put(field.value, leafNode, field.info);
     }
-    if (changing || field.info.type==MappingInfo::Proc) {
+    if ((changing || field.info.type==MappingInfo::Proc) && allowed) {
         // Do processing if required
         IOCSource::doPostProcessing(field.value, forceProcessing);
         return true;
diff --git a/ioc/singlesource.cpp b/ioc/singlesource.cpp
index 024f676..b10e269 100644
--- a/ioc/singlesource.cpp
+++ b/ioc/singlesource.cpp
@@ -29,3 +29,4 @@
+#include "sitehooks.h"
 #include "singlesource.h"
 #include "singlesrcsubscriptionctx.h"
 #include "credentials.h"
@@ -399,12 +400,10 @@ SingleSource::SingleSource()
     auto names(std::make_shared<std::set<std::string >>());
 
     //  For each record type and for each record in that type, add record name to the list of all records
-    DBEntry dbEntry;
-    for (long status = dbFirstRecordType(dbEntry); !status; status = dbNextRecordType(dbEntry)) {
-        for (status = dbFirstRecord(dbEntry); !status; status = dbNextRecord(dbEntry)) {
-            names->insert(dbEntry->precnode->recordname);
-        }
-    }
+    forEachRecord([&names](dbCommon* prec) {
+        if (site::isChannelAllowed(prec->name, nullptr))
+            names->insert(prec->name);
+    });
 
     allRecords.names = names;
 
@@ -437,3 +436,6 @@ void SingleSource::onCreate(std::unique_ptr<server::ChannelControl>&& channelCon
+    if (!site::isChannelAllowed(sourceName, channelControl->peerName().c_str()))
+        return;
+
     log_debug_printf(_logname, "Accepting channel for '%s'\n", sourceName);
 
     auto sInfo(std::make_shared<SingleInfo>(std::move(pDbChannel)));
@@ -469,3 +471,5 @@ void SingleSource::onCreate(std::unique_ptr<server::ChannelControl>&& channelCon
+        if (!site::isChannelAllowed(pv.name(), searchOperation.source()))
+            continue;
         if (!dbChannelTest(pv.name())) {
             pv.claim();
             log_debug_printf(_logname, "Claiming '%s'\n", pv.name());
diff --git a/ioc/sitehooks.cpp b/ioc/sitehooks.cpp
index 9855e11..ee6c614 100644
--- a/ioc/sitehooks.cpp
+++ b/ioc/sitehooks.cpp
@@ -22,3 +22,5 @@ namespace pvxs {
+void registerPvfilter();
+
 namespace {
 // Function-local static variables avoid init-order issues with site-specific registrars.
 std::vector<std::function<void()>>& hooksAtBeginning() {
@@ -57,3 +59,4 @@ void siteHookDispatch(initHookState state) noexcept
+    registerPvfilter();
     initHookRegister(siteHookDispatch);
 
     // If a site extension registered a replacement for the core-registered
diff --git a/setup.py b/setup.py
index 52000fe..6710e42 100755
--- a/setup.py
+++ b/setup.py
@@ -578,3 +578,4 @@ def define_DSOS(self):
+        "ioc/pvfilter.cpp",
         "ioc/field.cpp",
         "ioc/fielddefinition.cpp",
         "ioc/fieldname.cpp",
diff --git a/test/Makefile b/test/Makefile
index bfe42c3..e1a7240 100644
--- a/test/Makefile
+++ b/test/Makefile
@@ -157,3 +157,11 @@ endif
+TESTPROD_HOST += testpvfilter
+testpvfilter_SRCS += testpvfilter
+testpvfilter_SRCS += capturestd.cpp
+testpvfilter_SRCS += testioc_registerRecordDeviceDriver.cpp
+testpvfilter_LIBS = pvxsIoc pvxs $(EPICS_BASE_IOC_LIBS)
+TESTFILES += ../testpvfilter.db
+TESTS += testpvfilter
+
 TESTPROD_HOST += testqgroup
 testqgroup_SRCS += testqgroup
 testqgroup_SRCS += capturestd.cpp
